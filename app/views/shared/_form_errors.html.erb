<% content_for :form_errors do %>
  <p>
    Oops! <%= pluralize(object.errors.count, "error") %> prevented this action.
  </p>

  <ul>
    <% object.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
